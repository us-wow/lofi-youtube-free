# ===== 무료 기본 동작에 필요한 라이브러리 =====
# (※ torch·audiocraft·diffusers는 곡/그림 생성용 — 무거움. Colab이나 용량 넉넉한 PC에서만)

# --- 곡 생성 (무거움, ~5GB) ---
audiocraft==1.3.0

# --- 그림 생성 (무거움, ~3GB) ---
diffusers>=0.27
transformers>=4.40
torch>=2.2
accelerate>=0.29

# --- 가벼운 부품 (어디서나 OK) ---
requests>=2.31        # 트렌드 RSS 받기
feedparser>=6.0       # RSS 파싱
PyYAML>=6.0           # config.yaml 읽기
tqdm>=4.66            # 진행바
imageio-ffmpeg>=0.4.9 # ffmpeg 바이너리 번들 (영상 합성)

# --- 테스트 ---
pytest>=8.0
